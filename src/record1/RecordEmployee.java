package record1;


// record classes are meant to be simple data carriers.
// And they don't support  additional instance method
public record RecordEmployee(String firstName, String lastName, String email) {
}
